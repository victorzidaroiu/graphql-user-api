# User API

An API for managing a user

## Requirements

- Node 6.3
- MongoDB

## Setup

- `npm i -g nodemon`
- `cp .env.dev .env`
- `npm i`
- start `mongod` in another terminal tab

## Run

- npm run dev
